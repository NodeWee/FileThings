{
    "$schema": "../schemas/desktop-schema.json",
    "identifier": "default",
    "description": "Capability for the main window",
    "windows": ["main"],
    "permissions": [
        "core:default",
        "os:default",
        "process:allow-restart",
        "dialog:allow-open",
        "dialog:allow-save",
        "updater:default"
    ]
}
